<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.praktikum1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Quiz3"></activity>
        <activity android:name=".Quiz2" /> <!-- <activity android:name=".MultiLangual"></activity> -->
        <!-- <activity android:name=".PreAssessment"></activity> -->
        <!-- <activity android:name=".ScoreBoard"></activity> -->
        <!-- <activity android:name=".TravellingActivity"></activity> -->
        <!-- <activity -->
        <!-- android:name=".BiodataActivity" -->
        <!-- android:parentActivityName=".MainActivity" /> -->
        <activity android:name=".Quiz1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>